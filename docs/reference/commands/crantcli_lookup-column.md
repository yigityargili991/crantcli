# crantcli lookup-column

Find the closest EPG/PEG neuron's column (region) by position

## Synopsis

Find the closest EPG/PEG neuron to the given root ID (or position) by 3D Euclidean distance and print its resolved region value plus the nearest EPG/PEG root ID.

```
crantcli lookup-column [root_id] [flags]
```

## Options

```
  -h, --help         help for lookup-column
      --pos string   Position as x,y,z (e.g. 100.5,200.3,300.1)
```

## See also

* [crantcli](crantcli.md)	 - Query CRANT clonal raider ant connectome neurons and inject into Neuroglancer states

